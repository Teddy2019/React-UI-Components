import React from 'react';
import './Card.css';

function CardContent(){
    return (
        <h2>Get started with React</h2>
        <p>React makes it painless to create interactiv UIs. Design simple views for each state in your application.</p>
        <p>reactjs.org</p>
        )
};

export default CardContent;
